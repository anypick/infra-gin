
```shell
$ git remote add origin https://github.com/anypick/infra-gin.git
$ git config --local user.name anypick 
$ git config --local user.email yangy.hu@foxmail.com
$ git tag v0.0.0
$ git add .
$ git commit -m 'init project'
$ git push -f -u origin master
$ git push -f -u origin v0.0.0
``` 